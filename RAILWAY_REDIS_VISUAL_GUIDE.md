# Railway Redis Integration - Visual Guide

## 🎯 Goal
Add Redis to your Railway deployment for production-grade rate limiting.

---

## 📸 Step-by-Step Screenshots Guide

### Step 1: Access Railway Dashboard

```
┌─────────────────────────────────────────┐
│  🚂 Railway Dashboard                   │
│  https://railway.app/dashboard         │
└─────────────────────────────────────────┘
```

1. Log in to Railway
2. Navigate to your **TraderMind** project

---

### Step 2: Add Redis Database

```
┌─────────────────────────────────────────┐
│  TraderMind Project                     │
│                                         │
│  ┌──────────┐  ┌──────────┐           │
│  │  api-    │  │  quant-  │  ┌─────┐ │
│  │ gateway  │  │ engine   │  │ New │ │ ← Click here
│  └──────────┘  └──────────┘  └─────┘ │
└─────────────────────────────────────────┘
```

Click **"New"** button → Select **"Database"**

---

### Step 3: Select Redis

```
┌─────────────────────────────────────────┐
│  Add Database                           │
│                                         │
│  ┌────────────┐  ┌────────────┐       │
│  │ PostgreSQL │  │   MySQL    │       │
│  └────────────┘  └────────────┘       │
│                                         │
│  ┌────────────┐  ┌────────────┐       │
│  │  MongoDB   │  │   Redis    │ ← Click
│  └────────────┘  └────────────┘       │
└─────────────────────────────────────────┘
```

Click **"Add Redis"**

---

### Step 4: Wait for Deployment

```
┌─────────────────────────────────────────┐
│  Redis                                  │
│                                         │
│  Status: ⚙️  Deploying...              │
│                                         │
│  ▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░ 50%            │
│                                         │
│  Creating instance...                   │
└─────────────────────────────────────────┘

          ⬇️  Wait ~30 seconds  ⬇️

┌─────────────────────────────────────────┐
│  Redis                                  │
│                                         │
│  Status: ✅ Active                      │
│                                         │
│  Memory: 512 MB                         │
│  Region: us-west1                       │
└─────────────────────────────────────────┘
```

---

### Step 5: Verify REDIS_URL Variable

```
┌─────────────────────────────────────────┐
│  api-gateway Service                    │
│                                         │
│  [Deployments] [Variables] [Settings]  │
│        │                                │
│        └──── Click "Variables" tab     │
└─────────────────────────────────────────┘

            ⬇️  You should see  ⬇️

┌─────────────────────────────────────────┐
│  Environment Variables                  │
│                                         │
│  SUPABASE_URL                           │
│  https://xxx.supabase.co                │
│                                         │
│  REDIS_URL                          ✅  │
│  redis://default:***@redis.railway...  │
│                                         │
│  (Railway auto-injected this)           │
└─────────────────────────────────────────┘
```

---

### Step 6: Redeploy API Gateway

```
┌─────────────────────────────────────────┐
│  api-gateway Service                    │
│                                         │
│  [Deployments] [Variables] [Settings]  │
│        │                                │
│        └──── Click "Deployments"       │
└─────────────────────────────────────────┘

            ⬇️  Then  ⬇️

┌─────────────────────────────────────────┐
│  Latest Deployment                      │
│  #47 - main@abc1234                    │
│                                         │
│  ┌──────────┐                          │
│  │ Redeploy │ ← Click this            │
│  └──────────┘                          │
└─────────────────────────────────────────┘
```

---

### Step 7: Monitor Deployment Logs

```
┌─────────────────────────────────────────┐
│  Deployment Logs                        │
│                                         │
│  Building Docker image...               │
│  Pushing to registry...                 │
│  Starting container...                  │
│  ✅ Container started                   │
│                                         │
│  [inf] API Gateway started              │
│  [inf] RateLimiter Redis connected  ✅  │
│        └─── Look for this line!        │
└─────────────────────────────────────────┘
```

---

## ✅ Success Indicators

### ✔️ What You Should See

```bash
# In deployment logs:
[inf] API Gateway started
[inf] RateLimiter Redis connected        # ← Perfect!
[inf] WebSocket server ready
```

### ❌ What You Should NOT See

```bash
# No more continuous errors:
[err] RateLimiter Redis error             # ← Gone!
[err] RateLimiter Redis error             # ← Gone!
[err] RateLimiter Redis error             # ← Gone!
```

---

## 🎨 Your Final Architecture

```
┌─────────────────────────────────────────────────────┐
│                 Railway Project                     │
│                                                     │
│  ┌─────────────┐      ┌─────────────┐             │
│  │ api-gateway │─────▶│    Redis    │             │
│  │   (Node.js) │      │  (Database) │             │
│  └─────────────┘      └─────────────┘             │
│         │                    ▲                      │
│         │                    │                      │
│         │              REDIS_URL                    │
│         │         (auto-injected)                   │
│         │                                           │
│  ┌─────────────┐                                   │
│  │quant-engine │                                   │
│  │  (Optional) │                                   │
│  └─────────────┘                                   │
└─────────────────────────────────────────────────────┘
```

---

## 🔍 Troubleshooting

### Issue: Can't find "Add Redis" option

**Solution:**
- Check you're on a Pro plan (Redis requires Pro)
- Or use Upstash (see RAILWAY_REDIS_SETUP.md)

### Issue: REDIS_URL not appearing

**Solution:**
1. Refresh the page
2. Check Redis service is "Active"
3. Try removing and re-adding the variable manually

### Issue: Still seeing Redis errors

**Solution:**
1. Verify REDIS_URL format is correct
2. Restart the api-gateway service
3. Check Redis service status (should be green/active)

---

## 📊 Cost Breakdown

```
Railway Redis Pricing:
┌────────────────┬───────────┬──────────────┐
│ Plan           │ Storage   │ Price/month  │
├────────────────┼───────────┼──────────────┤
│ Starter        │ 512 MB    │ $5           │
│ Pro            │ 1 GB      │ $10          │
│ Team           │ 5 GB      │ $50          │
└────────────────┴───────────┴──────────────┘

Recommended: Starter ($5/month) is sufficient for most use cases
```

---

## 🚀 Next Actions

1. ✅ Redis deployed and connected
2. ✅ Rate limiting working globally
3. ✅ Logs are clean

**Optional enhancements:**
- [ ] Set up Redis metrics monitoring
- [ ] Configure backup/persistence settings
- [ ] Add Redis for session storage
- [ ] Implement caching layer

---

## 📞 Need Help?

- **Railway Docs:** https://docs.railway.app/databases/redis
- **Discord:** https://discord.gg/railway
- **Alternative Guide:** See RAILWAY_REDIS_SETUP.md

---

**Status:** Ready to deploy! 🎉
